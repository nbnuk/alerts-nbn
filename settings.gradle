rootProject.name='alerts-nbn'

// Inplace plugin config
// Set to true it needed only for development
boolean inplace = true

if (inplace) {
    include ':alerts'
    project(':alerts').projectDir = new File(settingsDir,'../alerts')
}

